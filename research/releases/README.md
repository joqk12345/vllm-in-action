# vLLM 版本追踪

每个上游版本创建一份 `vX.Y.Z.md`，从 `templates/release-impact.md` 复制。这里记录的重点不是完整翻译 release note，而是：

1. 哪些默认行为、API、兼容性或性能机制变了；
2. 哪些章节结论可能过期；
3. 哪些实验必须重跑；
4. 升级需要什么迁移或回退措施。

历史版本记录只追加，不覆盖；结论变化通过新记录与 decision log 解释。
